apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: 07-biossettings-reboot
spec:
  concurrent: false
  steps:
    - name: register-bmc
      try:
        - apply:
            file: ../shared/bmcsecret.yaml
        - apply:
            file: ../shared/bmc.yaml
    - name: assert-bmc-enabled
      try:
        - assert:
            resource:
              apiVersion: metal.ironcore.dev/v1alpha1
              kind: BMC
              metadata:
                name: (join('-', ['compatibility', $namespace]))
              status:
                state: Enabled
    - name: assert-server-available
      try:
        - assert:
            resource:
              apiVersion: metal.ironcore.dev/v1alpha1
              kind: Server
              metadata:
                name: (join('-', ['compatibility', $namespace, 'system-0']))
              status:
                state: Available
    - name: apply-biossettings
      try:
        - apply:
            file: biossettings-reboot.yaml
    - name: assert-biossettings-applied
      try:
        - assert:
            timeout: 30m
            resource:
              apiVersion: metal.ironcore.dev/v1alpha1
              kind: BIOSSettings
              metadata:
                name: (join('-', ['compatibility', $namespace, 'reboot']))
              status:
                state: Applied
    - name: assert-server-available-again
      try:
        - assert:
            timeout: 15m
            resource:
              apiVersion: metal.ironcore.dev/v1alpha1
              kind: Server
              metadata:
                name: (join('-', ['compatibility', $namespace, 'system-0']))
              spec:
                (serverMaintenanceRef || 'absent'): 'absent'
              status:
                state: Available
