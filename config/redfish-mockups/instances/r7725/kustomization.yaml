# Instance overlay for the R7725 iDRAC mockup client.
#
#   PLACEHOLDER_CLIENT -> /clients/R7725_iDRAC10_1_20_70_50_mockup_client
#   PLACEHOLDER_CLIENTS_MOUNT -> /redfish-clients
#   PLACEHOLDER_TLS_SECRET -> redfish-r7725-tls
#   PLACEHOLDER_BMC_SECRET_NAME -> redfish-r7725-mockup
#   PLACEHOLDER_HOSTNAME -> dell-r7725-mockup
#   PLACEHOLDER_BMC_IP -> 10.96.200.12
#   PLACEHOLDER_USERNAME / PLACEHOLDER_PASSWORD -> foo / bar
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: metal-operator-system

components:
- ../../components/mockup

namePrefix: redfish-r7725-

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/instance: r7725

patches:
# Resolves PLACEHOLDER_BMC_IP (Service clusterIP)
- target:
    kind: Service
    name: mockup
  patch: |-
    - op: replace
      path: /spec/clusterIP
      value: 10.96.200.12
# Resolves PLACEHOLDER_CLIENT, PLACEHOLDER_CLIENTS_MOUNT, and
# PLACEHOLDER_TLS_SECRET
- target:
    kind: Deployment
    name: mockup
  patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/args/1
      value: /clients/R7725_iDRAC10_1_20_70_50_mockup_client
    - op: replace
      path: /spec/template/spec/volumes/0/hostPath/path
      value: /redfish-clients
    - op: replace
      path: /spec/template/spec/volumes/1/secret/secretName
      value: redfish-r7725-tls
# Resolves PLACEHOLDER_TLS_SECRET
- target:
    kind: Certificate
    name: tls
  patch: |-
    - op: replace
      path: /spec/secretName
      value: redfish-r7725-tls
# Resolves BMC placeholders
- target:
    kind: BMC
    name: mockup
  patch: |-
    - op: replace
      path: /spec/bmcSecretRef/name
      value: redfish-r7725-mockup
    - op: replace
      path: /spec/hostname
      value: dell-r7725-mockup
    - op: replace
      path: /spec/access/ip
      value: 10.96.200.12
# Resolves BMCSecret credential placeholders
- target:
    kind: BMCSecret
    name: mockup
  patch: |-
    - op: replace
      path: /stringData/username
      value: foo
    - op: replace
      path: /stringData/password
      value: bar

replacements:
# Resolves SERVICE_NAME
- source:
    kind: Service
    name: mockup
    fieldPath: metadata.name
  targets:
  - select:
      kind: Certificate
      name: tls
    fieldPaths:
    - spec.dnsNames.0
    - spec.dnsNames.1
    options:
      delimiter: "."
      index: 0
      create: true
# Resolves SERVICE_NAMESPACE
- source:
    kind: Service
    name: mockup
    fieldPath: metadata.namespace
  targets:
  - select:
      kind: Certificate
      name: tls
    fieldPaths:
    - spec.dnsNames.0
    - spec.dnsNames.1
    options:
      delimiter: "."
      index: 1
      create: true
